image: node:latest

create-pages:
  script:
    - npm install
    - npx vite build
    - rm -rf public     
    - mv dist public     
  pages:
    publish: public
  artifacts:             
    paths:
      - public
  rules:
    - if: $CI_COMMIT_REF_NAME == $CI_DEFAULT_BRANCH